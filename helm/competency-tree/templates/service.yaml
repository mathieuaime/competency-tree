apiVersion: v1
kind: Service
metadata:
  name: {{ include "competency-tree.fullname" . }}
  labels:
    {{- include "competency-tree.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "competency-tree.selectorLabels" . | nindent 4 }}
